# The Deterministic Core

## Axiom

```
XOR computes. Canon interprets.
No floating interpretation decides the relation.
No projection changes the relation.
No local meaning changes the bitwise witness.
```

## What This Means

The irreducible computational center: one operation — XOR. Same inputs
always produce same output. On every platform, language, and scale.

## The Closure Test

```
0x20 ^ 0x5F ^ 0x80 ^ 0xFF = 0x00
```

Not interpretation. Bitwise computation.

## What Computes

```
XOR computes the relation witness.
Nothing else computes the relation witness.
Notation. Projection. Interpretation. Gauge. Display. None.
```

## Doctrine

```
OMI defines. SETCO classifies. Projection is not authority.
```
