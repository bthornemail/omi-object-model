# Tetragrammatron: Lawful Fold Governor

## Role

Tetragrammatron is the lawful fold governor. Validates exact nibble and
XOR closure conditions.

```
tetra = fourfold | gramma = grammar | tron = machine
```

## Fold Patterns

```
0x3C = 00111100  (static centered fold)
0x78 = 01111000  (shifted / carried bridge fold)
0x78 = 0x3C << 1
0x3C = 0x78 >> 1
```

Tetragrammatron performs the lawful fold/carry transition.

## Rules

```
Tetragrammatron carries the fold.
Tetragrammatron validates exact closure.
Does not frame, resolve, parse, project, or serialize.
```

## Doctrine

```
OMI defines. SETCO classifies. Projection is not authority.
```
