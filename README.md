# Omi Object Model

The Omi Object Model is the inside-out pure object/address/relation model.

## What it owns

- omi-notation
- omi-lisp
- omi-port
- null ring
- gauge ring
- Omicron
- Metatron
- Tetragrammatron
- Omnicron

## Core sentence

```
OMI defines the object model.
SETCO classifies how other things relate to it.
```

## Notation

The canonical notation surface is `omi---imo?O_o`.

- `omi---imo` is the folded root circulation.
- `?O_o` is the readable witness branch.
- `omi---imo?O_o` is the readable relation hinge.

## Source corpus

Generated from the neutral OMI-Lisp corpus at:

```
/home/main/omi/omi---imo/declarations
```

The corpus is not moved or modified. This repo is a manifest-driven emission.

## Build passes

```
Pass 1: manifest-driven generation (current)
Pass 2: semantic .omilisp parsing (deferred)
Pass 3: classified claim promotion (deferred)
```
